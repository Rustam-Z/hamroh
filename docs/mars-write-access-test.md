# Mars write-access test

This file is a one-off connectivity test — confirming that Mars (the
fleet-operator agent) can create a file, commit, and push to this repo
over HTTPS with the `gh`-delegated git credential helper.

Safe to delete once confirmed.
